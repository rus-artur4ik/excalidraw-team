server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # client-side routes (/b/:id, /admin) must fall back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
